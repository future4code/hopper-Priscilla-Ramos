// Chama a atebção o rootDir e o outDir que não estão configurados e que mudaram 
//do inicial para o configurado no slide. Sem eles configurados a nossa saída ficacria na pasta raíz, 
//assim como nossos arquivos de entrada. E os dois são ligados conforme a descrição, que diz que a
//rootDir é utilizada para controlar a estrutura do diretório outDir (Specify the root directory of input files. 
//Use to control the output directory structure with --outDir. */)